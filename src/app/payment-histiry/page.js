import React from 'react';
import PaymentHistory from '../Component/PaymentHistory';

const page = () => {
    return (
        <div>
           <PaymentHistory/>
        </div>
    );
};

export default page;